apiVersion: v1
kind: Service
metadata:
  name: {{ include "react-vite.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "react-vite.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
